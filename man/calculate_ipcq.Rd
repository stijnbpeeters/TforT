% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iPCQ_function.R
\name{calculate_ipcq}
\alias{calculate_ipcq}
\title{Calculate productivity costs from iPCQ responses}
\usage{
calculate_ipcq(
  dat,
  reference_year,
  currency = "EUR",
  input_period_weeks = 4,
  target_period_weeks = input_period_weeks,
  paid_work_hour_price = NULL,
  unpaid_work_hour_price = NULL,
  friction_period_days = NULL,
  na_action = c("error", "propagate")
)
}
\arguments{
\item{dat}{A data frame containing the required iPCQ columns. Additional columns are preserved.}
\item{reference_year}{Year used to select Dutch reference prices.}
\item{currency}{Currency label for results. Use \code{"EUR"} or
\code{"INT$"} for Dutch prices obtained from \code{tatooheene}. Other labels
are accepted when both hourly prices are supplied explicitly.}
\item{input_period_weeks}{Weeks represented by the reported counts.}
\item{target_period_weeks}{Period for which costs are returned.}
\item{paid_work_hour_price}{Optional paid-work price per hour.}
\item{unpaid_work_hour_price}{Optional unpaid-work replacement price per hour.}
\item{friction_period_days}{Optional friction period in calendar days.}
\item{na_action}{Either \code{"error"} or \code{"propagate"}.}
}
\value{
The original data frame with calculated hours, component costs, total costs,
and the parameters used.
}
\description{
Counts in \code{dat} describe \code{input_period_weeks}. They can be
extrapolated to \code{target_period_weeks}; long absence is calculated from
its start date and capped at the friction period.
}
